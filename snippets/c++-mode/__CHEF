# -*- mode: snippet -*-
# name: Basic testcase while loop
# uuid:
# Key: <ch
# condition: t
# --

#include <bits/stdc++.h>

using namespace std;

int main(int argc, char *argv[]) {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int testcase;
    cin >> testcase;
    while(testcase--){
        $0
    }
    return 0;
}
